# Placeholder implementation.
# Ersetze diese Funktionen durch deine echte Textextraktion (z.B. pytesseract, pdfminer.six, etc.).
def extract_text_from_pdf(path: str) -> str:
    try:
        # TODO: echte PDF-Textextraktion einbauen
        return ""
    except Exception:
        return ""

def extract_text_from_image(path: str) -> str:
    try:
        # TODO: echte OCR einbauen
        return ""
    except Exception:
        return ""
